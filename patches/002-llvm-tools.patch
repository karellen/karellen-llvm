diff --git a/llvm/cmake/modules/AddLLVM.cmake b/llvm/cmake/modules/AddLLVM.cmake
index a267166eb6c2..dd141de23dd2 100644
--- a/llvm/cmake/modules/AddLLVM.cmake
+++ b/llvm/cmake/modules/AddLLVM.cmake
@@ -1581,6 +1581,16 @@ if(NOT LLVM_TOOLCHAIN_TOOLS)
     llvm-profdata
     llvm-profgen
     llvm-symbolizer
+    llvm-dis
+    llvm-dwarfutil
+    # llvm-config reports the exact flags the dylib was built with; the IR tools
+    # are needed by downstream projects that compile against libLLVM.
+    llvm-config
+    llvm-as
+    llvm-extract
+    llvm-link
+    llc
+    opt
     # symlink version of some of above tools that are enabled by
     # LLVM_INSTALL_BINUTILS_SYMLINKS.
     addr2line
